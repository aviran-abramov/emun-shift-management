import { ScheduleProps } from "@/app/admin/schedule/[buildingId]/types";

export function ScheduleMobile({ availabilities }: ScheduleProps) {
  return (
    <div>
      <p>Mobile</p>
    </div>
  );
}
